import { CSSProperties } from 'react';

export const style: Record<string, CSSProperties> = {
  trackPad: {
    // background: 'white',
    fontSize: '16px',
    // minHeight: '300px',
    // minWidth: '300px',
    height: '100%',
    width: '100%',
  },
};
